spring:
  datasource:
    url: "jdbc:postgresql://localhost:11088/pbdb"
    username: "pbdb_user"
    password: "pbdb_pass"
    driverClassName: "org.postgresql.Driver"
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
